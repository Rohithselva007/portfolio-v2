# ── Local development .env ─────────────────────────────────────
# Copy this file to your project root and rename it .env
# Install python-dotenv: pip install python-dotenv
# Then add this to the TOP of manage.py and wsgi.py:
#   from dotenv import load_dotenv; load_dotenv()

# Core
SECRET_KEY=django-insecure-local-dev-key-change-on-render
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Email (local — console backend, no setup needed)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
CONTACT_EMAIL=rohithselva1@gmail.com

# Superuser (used by build.sh on Render)
DJANGO_SUPERUSER_USERNAME=SelvaRohith
DJANGO_SUPERUSER_EMAIL=rohithselva1@gmail.com
DJANGO_SUPERUSER_PASSWORD=changeme123
